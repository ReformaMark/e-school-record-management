import { schoolHeadData } from "./school-head-data";

export const schoolStats = {
    principal: `Dr. ${schoolHeadData.firstName} ${schoolHeadData.lastName}`,
    totalRegistrars: 5,
    totalTeachers: 50,
    totalStudents: 1000
}